- [x] Add filters as flags for delete/select/add/remove/reset
- [x] Fuzzy prompt for relationships should use the relationship name, not the
      resource name. For example, when creating a resource it should say
      "select base" not "select resource"
- [x] Provide relationships and/or attributes as flags for
      create/upload/download
- [x] No interactive mode (for scripts)
- [x] Use schema to provide better defaults during create/upload/download
- [x] Support get-related even when there is no dedicated URL (eg /teams/:id
      instead of /projects/:id/team)
- [ ] On upload, show raw JSON response
