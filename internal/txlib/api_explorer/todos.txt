- [ ] Fuzzy prompt for relationships should use the relationship name, not the
      resource name. For example, when creating a resource it should say
      "select base" not "select resource"
- [ ] Provide relationships and/or attributes as flags for
      create/upload/download
- [ ] Add filters as flags for delete/select/add/remove/reset
- [ ] Use schema to provide better defaults during create/upload/download
